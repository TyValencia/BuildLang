block multiply(num1, num2) sends int:
	send num1 * num2

block convert_binary(num) sends int:
	binary_num = "" 
    while num > 0:
        binary_num = str(num % 2) + binary_num 
        num = math.floor(num / 2)
    send binary_num 

main
	// more intuitive way to connect functions
	3, 5 |> multiply |> convery_binary